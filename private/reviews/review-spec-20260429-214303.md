# Hardcore Review: spec-caduceus-orchestrator-algorithm.md (Iteration B21)

- **Date**: 2026-04-29
- **Content Type**: Spec
- **Iteration**: B21 independent re-check
- **Reviewer Model(s)**: claude-opus-4.6, gpt-5.4, gpt-5.3-codex
- **Verdict**: NEEDS WORK

## Scores

| # | Dimension | Score | Min-Reviewer | Notes |
|---|-----------|-------|--------------|-------|
| 1 | Correctness | 3/10 | gpt-5.3-codex | Terminal-on-retry-fire path can drain without ring write |
| 2 | Completeness | 4/10 | gpt-5.3-codex | Spec does not define mandatory fallback ring writer |
| 3 | Security | 6/10 | gpt-5.3-codex | Observability/audit loss on missing terminal summary |
| 4 | Clarity | 4/10 | gpt-5.3-codex | Comments claim guaranteed prior write not actually guaranteed |
| 5 | Architecture | 2/10 | gpt-5.3-codex | `push_finished` requires `RunningEntry`, but retry-fire terminal path lacks one |
| 6 | SRP | 5/10 | gpt-5.3-codex | `on_retry_timer` duplicates map-drain logic across multiple branches |
| 7 | KISS / YAGNI / DRY | 5/10 | gpt-5.3-codex | Repeated drain blocks and overlapping normative statements |

**Average**: 4.1/10

## Findings

### 🔴 Critical
1. [L880–907] `on_retry_timer` Terminal arm drains maps but writes no guaranteed `recent_history_ring` summary. **Fix:** add canonical fallback terminal summary write in this arm before draining.
2. [L352–369] Postcondition text says no-ring-write branch is acceptable and implies prior canonical drains guarantee closure; this is not guaranteed for retry-pending terminalization. **Fix:** explicitly require ring-write behavior on retry-fire Terminal path.
3. [L1418–1426] Ring invariant #5 lists extra drain sites but not a guaranteed ring writer for retry-fire terminalization. **Fix:** make retry-fire Terminal arm a first-class terminal writer with single-write invariant.

### 🟡 Important
1. [L892–895] Comment references "§3.4 cascade call site" and assumes prior write; those cascade sites operate via §3.2 reconcile and cannot guarantee write once run has left `state.running`. **Fix:** replace with mandatory fallback-writer language.
2. [L786] Function-level docs should include all canonical writers (including defensive/fallback paths) to avoid stale invariants. **Fix:** tighten wording to list canonical writer set.

### 🟢 Suggestions
1. [L973+] Extract shared per-run drain helper to reduce copy/paste and proof burden.
2. [L2405–2409] Keep as-is; this clarification is correct and closed.

## Recommended Actions
1. Define `push_finished_from_retry_entry` (or equivalent) that can emit `FinishedRunSummary` when `RunningEntry` is absent.
2. Require `on_retry_timer` `TrackerClass::Terminal` arm to invoke it before state drain.
3. Update §3.1 postconditions and §6 ring invariants to codify exactly-one terminal summary across both `terminate_and_finish` and retry-fire fallback paths.
