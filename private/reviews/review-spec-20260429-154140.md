# Hardcore Review: spec-caduceus-agent-runner-contract.md

- **Date**: 2026-04-29
- **Content Type**: Spec
- **Iteration**: 14
- **Reviewer Model(s)**: gpt-5.4
- **Verdict**: NEEDS WORK

## Scores

| # | Dimension | Score | Min-Reviewer | Notes |
|---|-----------|-------|--------------|-------|
| 1 | Correctness | 3/10 | gpt-5.4 | Seq and runner_seq semantics still contradict themselves in normative text and tests. |
| 2 | Completeness | 5/10 | gpt-5.4 | Some fixes landed, but tests/spec prose do not fully lock them in. |
| 3 | Security | 8/10 | gpt-5.4 | No new obvious security regression in the touched areas. |
| 4 | Clarity | 5/10 | gpt-5.4 | Multiple stale phrases reintroduce the old mental model. |
| 5 | Architecture | 6/10 | gpt-5.4 | Transport-path unification is good, but ACP path now contradicts core queue/stamp contract. |
| 6 | SRP | 8/10 | gpt-5.4 | Sections remain reasonably separated. |
| 7 | KISS / YAGNI / DRY | 6/10 | gpt-5.4 | Duplicate/stale seq semantics across sections create avoidable complexity. |

**Average**: 5.9/10

## Findings

### 🔴 Critical
1. [§3.2 L411-L425, §3.5/Z-24 L953-L967, §6 T-13 L2036-L2042] — Fix #1 is not actually closed. The spec says “first-frame seq != 1 → gap”, but seq=0 is still special-cased to `stutter`; T-13(c) says “any value other than 1” should be `gap`. **Fix:** On first-frame validation, classify every `event.seq != 1` as `kind_detail = "gap"`; reserve the `seq == 0 => stutter` rule for post-first-frame regressions only, or delete the seq=0 stutter carveout entirely.
2. [§3.4 ACP L654-L683, esp. L660-L662/L681-L682; §4.1 L1042-L1043] — Fix #2 is half-reverted. ACP path and the §4.1 heading still say `runner.next_runner_seq()` / `runner_seq` stamping happens in `validate_and_wrap`, which contradicts Z-23 post-Ok stamping. **Fix:** Replace those lines with text that says ACP frames share the same seq validation and `forward_to_daemon` path, but `runner_seq` is stamped only after `forward_to_daemon` returns `Ok`.
3. [§3.5/Z-24 L947-L949 vs §3.2 L418-L420 vs §6 T-13 L2034-L2042] — The spec disagrees on `seq < high_water`: Z-24 calls it `"regression"`, but algorithm + T-13 call it `"stutter"`. **Fix:** Pick one taxonomy and make §3.2, Z-24, and T-13 identical. If the intent is duplicate-only=`stutter`, then change the algorithm/T-13 to emit `"regression"` for `< high_water`.

### 🟡 Important
1. [§4.3 L1295-L1314, §6 T-3 L1907-L1912] — Fix #3 is only partially locked in: implementation prose adds `cause: "delta_only_absolute_baseline"`, but T-3 does not assert that cause. **Fix:** Add “Assert diagnostic includes `cause = "delta_only_absolute_baseline"`.” to T-3.
2. [Z-24 L949-L951] — “runner stamps it on every wrapped event” is stale after the Option/post-Ok change; dropped/coalesced wraps do not get a wire-visible stamp. **Fix:** Change to “runner stamps it on every wrapped event that survives delivery to the daemon.”

### 🟢 Suggestions
1. [§4.4 / ACP] — Add one short cross-reference sentence: “ACP translation does not own `runner_seq`; only the shared post-Ok delivery path does.”

## Recommended Actions
1. Fix the seq taxonomy first (#1 and #3); they currently block convergence.
2. Purge all pre-Ok `runner_seq` wording from ACP and §4.1.
3. Strengthen T-3 so the new `cause` field is tested, not just narrated.
