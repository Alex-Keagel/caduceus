# Hardcore Review: spec-orchestrator-status-snapshot.md

- **Date**: 2026-04-28 01:05
- **Content Type**: Spec
- **Iteration**: 1
- **Reviewer Model(s)**: claude-opus-4.7, claude-sonnet-4.6, gpt-5.4, gpt-5.3-codex, gpt-5.2 (synthesized)
- **Verdict**: NEEDS WORK

## Scores

| # | Dimension | Score | Min-Reviewer | Notes |
|---|-----------|-------|--------------|-------|
| 1 | Correctness | 5/10 | gpt-5.4/gpt-5.3-codex | Reconnect contract + stream ordering contradictions |
| 2 | Completeness | 6/10 | opus/sonnet/gpt | Missing stale-fingerprint trigger and request option shape |
| 3 | Security | 5/10 | opus/gpt | Unbounded raw/patch payloads; host-field disclosure by default |
| 4 | Clarity | 6/10 | gpt/sonnet | bytes vs chars mismatch; overloaded protocol prose |
| 5 | Architecture | 7/10 | gpt/sonnet | Strong core split, but reconnect/backlog model under-specified |
| 6 | SRP | 7/10 | gpt/sonnet | Mostly clean boundaries; some ownership leaks to spec #1 fields |
| 7 | KISS | 6/10 | gpt/sonnet | Heavy duplication and open-question residue in normative path |

**Average**: 6.0/10

## Findings

### 🔴 Critical
1. **[§3.4 Force-resync triggers]** Closed set (a,b,c) misses same-boot stale fingerprint when `since_stream_seq=None`; daemon can return `UpToDate` on stale local state.
2. **[§3.4 Stream protocol + worked example]** First-delta rule says `delta.stream_seq > ack.stream_seq`, but worked example replays deltas `10,11` after `ack.stream_seq=11`.
3. **[§4.8 / §3.3]** `RateLimitBlob.raw` and `fields_changed` have no mandatory size bounds; this is broadcast amplification and DoS risk.
4. **[§4.1]** `pid` and required `workspace_path` expose host/process topology without mandatory redaction baseline.

### 🟡 Important
1. **[§3.2 + §4.5 + §4.9]** Request contract lacks options even though spec defines `RunDetailOptions` and `BudgetTooSmall` behavior.
2. **[§3.1 + §4.4]** Option name says `last_event_max_chars`, normative truncation is byte-based.
3. **[§4.2.1]** Truncation ownership claims mutate `RetryEntry` from spec #1; boundary should stay in spec #1 writer path.

### 🟢 Suggestions
1. **[§1.2 vs §7]** Duplicated out-of-scope lists increase drift.
2. **[§8]** Several “open questions” are already settled and should move to a decision log.

## Recommended Actions
1. Fix reconnect correctness first (§3.4 clauses and stream protocol).
2. Add hard payload budgets + redaction defaults (§3.3, §4.1, §4.8).
3. Normalize request/option contract and byte terminology.
