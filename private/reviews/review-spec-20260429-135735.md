# Hardcore Review: spec-caduceus-orchestrator-algorithm.md

- **Date**: $(date '+%Y-%m-%d %H:%M')
- **Content Type**: Spec
- **Iteration**: 12
- **Reviewer Model(s)**: claude-opus-4.7, gpt-5.4, orchestrator synthesis
- **Verdict**: NEEDS WORK

## Scores

| # | Dimension | Score | Min-Reviewer | Notes |
|---|-----------|-------|--------------|-------|
| 1 | Correctness | 6/10 | synthesized | Iter-11 fixes verified, but retry path can livelock when `fetch_by_ids` returns a run and `revalidate` rejects it. |
| 2 | Completeness | 6/10 | synthesized | No terminal/claim-release branch is specified for retry-time `revalidate_raced`. |
| 3 | Security | 9/10 | synthesized | No new security regression found in the verified fixes. |
| 4 | Clarity | 8/10 | synthesized | The new fixes are clear, but the retry contract still implies cleanup that the pseudocode does not guarantee. |
| 5 | Architecture | 7/10 | synthesized | Claim ownership across exit→retry is sound, but the deferred retry branch lacks a terminating state transition. |
| 6 | SRP | 9/10 | synthesized | Responsibilities remain well partitioned. |
| 7 | KISS / YAGNI / DRY | 8/10 | synthesized | Minimal targeted fixes; retry branch semantics remain slightly over-coupled. |

**Average**: 7.6/10

## Findings

### 🟡 Important
1. [spec-caduceus-orchestrator-algorithm.md:844-867, 527-533, 616-618] — `on_retry_timer` can livelock a claimed run forever when `fetch_by_ids` still returns the run but `dispatch_run` defers with `reason: "revalidate_raced"` (for example, closed/reassigned between fetch and revalidate). In this branch the code keeps `claimed` + `retry_attempts` and reschedules, but reconcile only examines `running`, so nothing ever releases the claim. **Fix:** Add a retry-timer branch that treats non-dispatchable revalidation as terminal-to-retry logic, e.g. release `claimed`/`retry_attempts` (and any per-run token maps if no future finalize can occur), or explicitly classify/refetch and route closed/reassigned runs through the appropriate terminal/Neither cleanup path instead of blindly rescheduling.

## Recommended Actions
1. Fix the retry-time `revalidate_raced` branch so it does not reschedule indefinitely while the run remains claimed.
