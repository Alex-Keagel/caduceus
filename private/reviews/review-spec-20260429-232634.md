# Hardcore Review: docs/specs/spec-multi-repo-workspace-model.md

- **Date**: 2026-04-29 23:26
- **Content Type**: Spec
- **Iteration**: 24
- **Reviewer Model(s)**: claude-opus-4.6, gpt-5.4, gpt-5.3-codex, orchestrator synthesis
- **Verdict**: NEEDS WORK

## Scores

| # | Dimension | Score | Min-Reviewer | Notes |
|---|-----------|-------|--------------|-------|
| 1 | Correctness | 4/10 | claude-opus-4.6 | Queue-drain summaries still contradict the canonical §5B.2 step 7 rule. |
| 2 | Completeness | 5/10 | claude-opus-4.6 | `OrphanPending -> Active` exists in §4.3 but is omitted or muddled in other summaries/diagram. |
| 3 | Security | 8/10 | gpt-5.4 | Canonical text preserves the step-2 validation invariant; issue is contradictory summaries, not the core rule. |
| 4 | Clarity | 4/10 | claude-opus-4.6 | Same behavior is summarized in mutually inconsistent ways (`step 2`, `step 2/3`, `step 3`). |
| 5 | Architecture | 7/10 | gpt-5.4 | Canonical queue-as-reclaim-path design is coherent, but duplicated summaries leak contradictory control flow. |
| 6 | SRP | 8/10 | gpt-5.4 | Ownership boundaries are mostly clean; contradictions are documentation scatter rather than design collapse. |
| 7 | KISS / YAGNI / DRY | 5/10 | gpt-5.3-codex | Repeated lifecycle/queue summaries have drifted and now fight the canonical text. |

**Average**: 5.9/10

## Findings

### 🔴 Critical
1. [docs/specs/spec-multi-repo-workspace-model.md:1471] — `OrphanPending` exit summary says reconcile drain re-enters §3.6 from `step 2/3`, but the canonical drain procedure says start at **step 2** and skip only steps 1 and 4. **Fix:** Replace `re-enters §3.6 from step 2/3` with `re-enters §3.6 from step 2 (skipping only step 4's layered liveness probe after the drain has already performed the §3.6 step 1 lifecycle mutation)`.
2. [docs/specs/spec-multi-repo-workspace-model.md:1780] — Crash-points table says `cleanup_workspace(_, OrphanReclaim, Some(&row))` re-enters §3.6 at **step 3** and says the liveness probe was already performed by the orchestrator at enqueue time. Both are wrong for the five-source queue model; startup-recovery sources are not orchestrator-driven, and step 2 must still run. **Fix:** Replace the action text with: ``cleanup_workspace(_, OrphanReclaim, Some(&row))` re-enters §3.6 at step 2; only step 4 is skipped. The enqueue site already supplied the positive-disproof rationale for the queue entry (per §5B.2 step 7's five-source table), so steps 2, 3, 5, 5a, 6, 7, 8, and 9 run unchanged.`
3. [docs/specs/spec-multi-repo-workspace-model.md:1811-1812] — Startup-recovery narrative repeats the same wrong summary: drain `re-runs §3.6 from step 3`. **Fix:** Replace `step 7's drain then re-runs §3.6 from step 3 (skipping step 4)` with `step 7's drain then re-runs §3.6 from step 2, skipping only step 4`.

### 🟡 Important
1. [docs/specs/spec-multi-repo-workspace-model.md:1781] — Crash-points table omits the alive recovery path for `OrphanPending`; it mentions only `positively disproved -> enqueue OrphanReclaim`. **Fix:** Prepend `Alive => transition back to Active;` before `Once liveness positively disproved...`.
2. [docs/specs/spec-multi-repo-workspace-model.md:2010-2015] — Reconcile summary mentions only `OrphanPending` positive disproof -> enqueue, omitting `Alive -> Active`. **Fix:** Replace the sentence with `Independently, the same reconcile pass re-evaluates rows in `OrphanPending` and `CleanupFailed` per §5B.1 (re-running §3.6 step 4 layered liveness for `OrphanPending`; bounded retry for `CleanupFailed`); `OrphanPending` rows that probe Alive transition back to `Active`, while rows whose liveness becomes positively disproved are enqueued onto `OrphanReclaim` rather than directly calling `cleanup_workspace`, so the queue remains the single canonical reclaim path.`
3. [docs/specs/spec-multi-repo-workspace-model.md:2020-2061] — Lifecycle diagram shows `Active -> OrphanPending` and `OrphanPending -> CleaningUp` but no return edge `OrphanPending -> Active`, so it visually contradicts §4.3. **Fix:** Add an arrow from `OrphanPending` back to `Active` labeled `liveness alive / heartbeat fresh (reconcile)`.
4. [docs/specs/spec-multi-repo-workspace-model.md:1422-1433] — Startup-recovery summary scopes rows in `Active` or `OrphanPending`, then says fresh-heartbeat rows `remain Active`; for an `OrphanPending` row this should be a transition back to `Active`, not `remain`. **Fix:** Replace `the row remains `Active`` with `the row is `Active` (unchanged if already Active; transitioned back to Active if it was `OrphanPending`)`.

### 🟢 Suggestions
1. [docs/specs/spec-multi-repo-workspace-model.md:1960-1963] — The canonical five-source statement is correct; add a brief forward pointer from every informative summary table to these lines to reduce future drift.
2. [docs/specs/spec-multi-repo-workspace-model.md:913] — Keep this paragraph as the single short summary for the bypass rule and trim redundant restatements elsewhere.

## Recommended Actions
1. Fix the three queue-drain summaries that still say `step 3` / `step 2/3`.
2. Add the missing `OrphanPending -> Active` path to the crash-points table, reconcile summary, startup summary, and diagram.
3. Keep §5B.2 step 7 as the only normative source for enqueue-source counting and bypass scope.
