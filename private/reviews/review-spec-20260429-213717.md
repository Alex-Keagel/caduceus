# Hardcore Review: spec-caduceus-orchestrator-algorithm.md

- **Date**: 2026-04-29
- **Content Type**: Spec
- **Iteration**: 1
- **Reviewer Model(s)**: claude-sonnet (single-pass)
- **Verdict**: NEEDS WORK

## Scores

| # | Dimension | Score | Min-Reviewer | Notes |
|---|-----------|-------|--------------|-------|
| 1 | Correctness | 5/10 | orchestrator | B21/C20 trace has an unproven guarantee and contradictory comments. |
| 2 | Completeness | 6/10 | orchestrator | Missing explicit normative handling for retry-only terminal/neither ring semantics. |
| 3 | Security | 8/10 | orchestrator | No direct security flaw in reviewed scope. |
| 4 | Clarity | 6/10 | orchestrator | Multiple stale section/line references misdirect implementers. |
| 5 | Architecture | 7/10 | orchestrator | Ring-writer ownership and retry-window terminalization are underspecified. |
| 8 | SRP | 8/10 | orchestrator | Responsibilities mostly separated. |
| 9 | KISS / YAGNI / DRY | 7/10 | orchestrator | Duplicated drain logic + contradictory explanatory text increases complexity. |

**Average**: 6.7/10

## Findings

### 🔴 Critical
1. [L892-L895, L426-L503, L708-L710] — The spec claims the ring entry for `on_retry_timer` Terminal/Neither is written by earlier cascade call sites, but those call sites iterate `state.running`, and this path is entered only after `on_worker_exit` removed the run from `state.running`. No normative path guarantees prior ring insertion. **Fix:** define explicit ring behavior for retry-pending terminalization (either write a terminal summary here or explicitly declare no ring summary and test it).

### 🟡 Important
1. [L792-L793] — "ring invariant #5 in §3.5" is stale; ring invariants are in §4 data-shape docstring. **Fix:** change to "ring invariant #5 in §4".
2. [L988] — "Ring Invariant #5 in §6" is stale. **Fix:** change to "Ring Invariant #5 in §4".
3. [L2256] — "Ring Invariant #5 (§6)" is stale. **Fix:** change to "Ring Invariant #5 (§4)".
4. [L892-L894] — References "§3.4 cascade call site" but listed cascade sites are in §3.2. **Fix:** change "§3.4" to "§3.2".
5. [L2230] — Deferred-branch line pointer "~L958" is stale (tail reschedule is now ~L1017-L1025). **Fix:** update line pointer to "~L1017-L1025" or remove numeric line anchors.
6. [L362-L363] — "Each removes the run from running and drains tokens together" is inaccurate: `runs.is_empty()` and defer-cap abandon may have no `running` entry to remove. **Fix:** rewrite to distinguish "drains maps" from "removes running".
7. [L1428-L1430] — Over-strong claim that tokens are preserved by ring entry in all listed drain paths; not true for no-ring drain branches. **Fix:** scope preservation claim to terminal-finalize writers only.

### 🟢 Suggestions
1. Add a dedicated test in §6 for retry-only terminal/neither path clarifying whether ring entry is required or forbidden.

## Recommended Actions
1. Resolve C20 explicitly: add normative text + pseudocode for retry-pending terminal/neither ring semantics.
2. Correct stale section/line pointers and over-claims.
3. Add one test that locks the chosen behavior.
