# Hardcore Review: spec-multi-repo-workspace-model.md

- **Date**: 2026-04-29
- **Content Type**: Spec
- **Iteration**: 13
- **Reviewer Model(s)**: claude-opus-4.7, claude-sonnet-4.6, gpt-5.4, gpt-5.3-codex, gpt-5.2
- **Verdict**: NEEDS WORK

## Scores

| # | Dimension | Score | Min-Reviewer | Notes |
|---|-----------|-------|--------------|-------|
| 1 | Correctness | 6/10 | gpt-5.3-codex | §3.5 step 1b / step 10 still conflict on 10e vs 10c behavior; stale `Failed` remains. |
| 2 | Completeness | 6/10 | gpt-5.3-codex | Diagram/state coverage still misses one `CleaningUp -> OrphanPending` path and rollback text is not fully closed. |
| 3 | Security | 9/10 | all | No new security regressions in the addressed sections. |
| 4 | Clarity | 6/10 | gpt-5.3-codex | Step 8.5 is mislabeled; diagram labels remain misleading. |
| 5 | Architecture | 8/10 | claude-opus-4.7 / gpt-5.2 / gpt-5.3-codex | State machine is sound overall; rendering lags normative text. |
| 6 | SRP | 8/10 | claude-opus-4.7 | Section responsibilities remain clean. |
| 7 | KISS / YAGNI / DRY | 8/10 | all | Mostly tight; minor duplication / stale naming remains. |

**Average**: 7.3/10

## Findings

### 🔴 Critical
1. [§3.5 step 10 / lines 663-678] — Rollback-side mid-walk failure still conflicts with step 1b. Step 1b says 10e retains the row as `Status::CleanupFailed` and skips 10c; step 10 still performs 10c before 10e can preserve the row. **Fix:** in step 10a/10e, add explicit normative text: `On mid-walk unlinkat failure in sub-step (a), transition the placeholder row to Status::CleanupFailed with Error::CleanupIncomplete (rollback-side), skip sub-steps (b) and (c), proceed directly to sub-step (d), and after lock release return the original triggering error.`

### 🟡 Important
1. [§3.5 line 397] — `step 8.5, the placeholder-row insert` is factually wrong; 8.5 is leaf-ownership handoff, placeholder insert is step 1.4. **Fix:** `Steps 3–8 (and step 8.5, the leaf-ownership handoff) on failure enter the rollback path at step 10.`
2. [§3.5 lines 457-460] — Stale non-existent status token remains: ``Cleaned` / `Failed``. **Fix:** ``Cleaned` / `CleanupFailed``.
3. [§5B.3 lines 1651-1677] — Diagram still misroutes / under-specifies `OrphanPending`: the `Active -> OrphanPending` label says `liveness disproved`, but disproved liveness goes to `CleaningUp`; `OrphanPending` is for inconclusive liveness and cleanup-side reclaim deferral. **Fix:** relabel the `Active -> OrphanPending` edge to `liveness inconclusive (reconcile pre-filter)` and add a `CleaningUp -> OrphanPending` edge labeled `§3.6 step 5/5a deferral`.

### 🟢 Suggestions
1. [§4.4 error table] — Consider adding an explicit rollback-side `ParentRevalidationFailed` entry or expanding the existing row to mention §3.5 step 10a, for symmetry with the rollback-side `CleanupOwnershipFailed` row.
2. [DRY] — Name the three rollback-retain conditions once and reference them, instead of repeating the full list in §1b, I-7, §5A.5, T-6, and T-12.

## Recommended Actions
1. Fix the 10e/10c normative contradiction first.
2. Correct the stale `Failed` token and the 8.5 mislabel.
3. Align the informative diagram with the normative state table.
