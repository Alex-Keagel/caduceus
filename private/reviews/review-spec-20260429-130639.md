# Hardcore Review: spec #2 agent-runner-contract (Iter-9)

- **Date**: 2026-04-29
- **Content Type**: Spec
- **Iteration**: 9
- **Reviewer Model(s)**: claude-sonnet (orchestrator pass)
- **Verdict**: NEEDS WORK

## Scores

| # | Dimension | Score | Min-Reviewer | Notes |
|---|-----------|-------|--------------|-------|
| 1 | Contract correctness | 8/10 | orchestrator | §3.1 validator placement and T-11(d) are correctly added. |
| 2 | Fail-closed security | 8/10 | orchestrator | Invalid/unset daemon value now fails closed with explicit refusal path. |
| 3 | Algorithmic completeness | 8/10 | orchestrator | Bidirectional env↔handshake path and runtime enforcement are coherent. |
| 4 | Test contract coverage | 8/10 | orchestrator | T-11(a–d) now covers both caller-side gate and daemon-default invalid-value path. |
| 5 | Cross-section consistency | 7/10 | orchestrator | I-9.2 still labels invalid/unset as "workflow-config error" though T-11(d) treats daemon-default bug. |
| 6 | Diagnostic operability | 7/10 | orchestrator | `reserved_env_invalid_value` not listed in daemon-internal diagnostic examples list near §4.1 message taxonomy block. |
| 7 | Implementability clarity | 8/10 | orchestrator | Implementation path is concrete and executable. |

**Average**: 7.7/10

## Findings

### 🟡 Important
1. [spec-caduceus-agent-runner-contract.md:1609-1611] Invalid/unset `CADUCEUS_DECLARES_TOOL_USE` is called a "workflow-config error", but T-11(d) explicitly frames the reachable invalid-value path as daemon-default-layer bug simulation.
   **Fix:** Replace:
   `workflow-config error and the daemon MUST refuse the spawn with a`
   with:
   `configuration error (including daemon-default-layer bugs) and the daemon MUST refuse the spawn with a`

2. [spec-caduceus-agent-runner-contract.md:985-993] Daemon-internal diagnostic examples list omits `reserved_env_invalid_value`, reducing observability discoverability and creating cross-section drift vs §3.1/I-9.2/T-11(d).
   **Fix:** In the diagnostic examples list, insert `reserved_env_invalid_value` adjacent to `reserved_env_override` and `reserved_env_prefix`.

### 🟢 Suggestions
1. [spec-caduceus-agent-runner-contract.md:1611] Use explicit variant in I-9.2 prose (`SpawnRefused::ReservedEnvInvalidValue`) instead of generic `SpawnRefused` for tighter implementation contract.

## Recommended Actions
1. Apply the two Important text fixes.
2. Re-run final hardcore pass; expected to reach all dimensions >=8.
