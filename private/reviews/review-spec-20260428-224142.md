# Hardcore Review: spec-caduceus-orchestrator-algorithm cross-spec check

- **Content Type**: Spec
- **Iteration**: 1
- **Verdict**: NEEDS WORK

## Scores

| # | Dimension | Score | Notes |
|---|-----------|-------|-------|
| 1 | Correctness | 5/10 | Transient WorkSource failure path can incorrectly reap all running runs; field mismatch in pseudocode. |
| 2 | Completeness | 6/10 | Undefined max_concurrency-downshift behavior, missing disconnect lifecycle in orchestrator state. |
| 3 | Security | 6/10 | Workspace canonicalization handoff contract mismatch across specs; shell-wrap risk boundary underspecified. |
| 4 | Clarity | 7/10 | Mostly clear, but some reason strings and state transitions are ambiguous across specs. |
| 5 | Architecture | 6/10 | Cross-spec ownership seams are strong but not fully reconciled (snapshot vs orchestrator state shape). |
| 6 | SRP | 7/10 | Mostly clean boundaries; several ownership seams still unresolved in normative text. |
| 7 | KISS/YAGNI/DRY | 7/10 | Good restraint overall, but unresolved seams create duplicate/implicit behavior definitions. |

## Key Critical Findings

1. `spec-caduceus-orchestrator-algorithm.md:294` uses `unwrap_or_default()` on `fetch_by_ids`, which can cause false orphan reaping when WorkSource is transiently unavailable.
2. `spec-caduceus-orchestrator-algorithm.md:307` updates non-existent `entry.snapshot` field; data shape defines `run_snapshot`.
3. Cross-spec workspace path identity mismatch between spec #1 dispatch path derivation and spec #3 multi-repo workspace model.

