# Hardcore Review: spec-caduceus-orchestrator-algorithm.md iter-15 fix verification

- **Date**: $(date '+%Y-%m-%d %H:%M')
- **Content Type**: Spec
- **Iteration**: 15
- **Reviewer Model(s)**: gpt-5.4 orchestrator, claude-opus-4.7 cross-check, gpt-5.4 cross-check
- **Verdict**: NEEDS WORK

## Scores

| # | Dimension | Score | Min-Reviewer | Notes |
|---|-----------|-------|--------------|-------|
| 1 | Correctness | 8/10 | gpt-5.4 cross-check | Fix 2 is mechanically complete; Fix 1 still says `unset (or 0)` instead of seeding `dispatch_defer_attempts[R1] = 0` exactly. |
| 2 | Completeness | 7/10 | gpt-5.4 cross-check | T-Z9 still leaves the defer-counter precondition and timer-message setup slightly ambiguous. |
| 3 | Security | 9/10 | all | Startup validation now fails closed before state allocation. |
| 4 | Clarity | 8/10 | gpt-5.4 cross-check | Setup is much clearer, but `unset (or 0)` is a two-form precondition for a test that was supposed to pin one exact seed state. |
| 5 | Architecture | 9/10 | all | `validate_dispatch_config` owns invariant formation; `start_service` owns logging+abort ordering. |
| 6 | SRP | 9/10 | all | Responsibilities remain cleanly separated. |
| 7 | KISS / YAGNI / DRY | 8/10 | gpt-5.4 cross-check | The validation fix is minimal; T-Z9 still has a small ambiguity that invites test harness variance. |

**Average**: 8.3/10

## Findings

### 🔴 Critical
None.

### 🟡 Important
1. [spec-caduceus-orchestrator-algorithm.md:2081-2083] — T-Z9 does not mechanically match the claimed fix `dispatch_defer_attempts[R1] = 0`; it permits `unset (or 0)`. That is behaviorally close because the implementation uses `.entry(...).or_insert(0)`, but it is not the exact seeded precondition B15 claims to have pinned. **Fix:** Replace step 4 with: `4. **Defer counter seeded.** \`state.dispatch_defer_attempts.get(&R1) == Some(&0)\` at the start of the test, so the counter transitions \`0 → 1 → … → N\` are observed from an explicitly materialized initial state.`
2. [spec-caduceus-orchestrator-algorithm.md:2073-2075] — The setup references an “in-flight `Cmd::RetryRun { run_id: R1, token: T1 }` message” but does not explicitly say the test harness enqueues that message before the first fire. **Fix:** Replace `in-flight` wording with: `the test harness has already enqueued \`Cmd::RetryRun { run_id: R1, token: T1 }\`, so the freshness check at the top of \`on_retry_timer\` (§3.5) admits each fire instead of dropping it as superseded (I-4).`

### 🟢 Suggestions
1. [spec-caduceus-orchestrator-algorithm.md:2115-2127] — Config-validation variant is solid; no further changes needed.

## Recommended Actions
1. Tighten T-Z9 step 4 from `unset (or 0)` to an exact seeded `Some(&0)` state.
2. Make the initial `Cmd::RetryRun { run_id: R1, token: T1 }` enqueue explicit in setup text.
3. Re-run review after those two wording fixes; Fix 2 already closes cleanly.
