# Hardcore Review: spec-caduceus-agent-runner-contract.md

- **Date**: 2026-04-29 13:34
- **Content Type**: Spec
- **Iteration**: 10
- **Reviewer Model(s)**: gpt-5.4, gpt-5.3-codex, claude-sonnet-4.6, gpt-5.2
- **Verdict**: NEEDS WORK

## Scores

| # | Dimension | Score | Min-Reviewer | Notes |
|---|-----------|-------|--------------|-------|
| 1 | Correctness | 7/10 | gpt-5.2 | T-8 summary does not fully encode all §4.5 next-actions. |
| 2 | Completeness | 6/10 | gpt-5.2 | T-8 omits explicit assertions for `DaemonShuttingDown` and `SessionMismatch`; `ReattachWindowExpired` lacks explicit resubscribe assertion. |
| 3 | Security | 7/10 | gpt-5.4 | No new security hole, but lowest-pass score remained 7 due to spec-level review conservatism. |
| 4 | Clarity | 7/10 | gpt-5.2 | `Z6-B1` is undefined; T-8 prose invites misread against §4.5. |
| 5 | Architecture | 8/10 | gpt-5.2 | Strong overall; remaining drift is local test-contract wording. |
| 6 | SRP | 9/10 | gpt-5.4/gpt-5.2 | Responsibilities stay crisp. |
| 7 | KISS / YAGNI / DRY | 8/10 | gpt-5.4/gpt-5.2 | Tight overall; one pseudocode double-call may confuse implementers. |

**Average**: 7.4/10

## Findings

### 🔴 Critical
1. [§6 T-8, lines 1747-1752] — The test says it drives every reattach variant, but the asserted next-actions do not cover the full §4.5 matrix. `DaemonShuttingDown` is listed but not asserted; `SessionMismatch` is listed as skip-or-stub but no required assertion is stated; `ReattachWindowExpired` is grouped under “no retry” without explicitly asserting the required spec #4 §3.4 resubscribe. **Fix:** Expand T-8 so every listed variant has an explicit expected action mirroring §4.5.

### 🟡 Important
1. [§6 T-11(c), line 1774] — `Z6-B1` is undefined in this spec/repo, making the citation non-actionable. **Fix:** Replace `per Z6-B1` with a concrete in-document reference such as `per §3.1 / I-9 reserved-env-override gate`.
2. [§3.1 pseudocode, lines 143 and 157] — `hooks.exported_env(run)` is invoked twice. In pseudocode this is probably intended as the same snapshot, but repeated evaluation can imply divergent values if hooks are non-deterministic. **Fix:** Capture hook-exported env once, then reuse that captured map for both merge and validation.

### 🟢 Suggestions
1. [§3.1 comment, line 122] — `Hooks always win` is slightly over-broad because reserved `CADUCEUS_*` keys fail closed rather than being shadowed. **Fix:** qualify the comment with `for non-reserved keys`.

## Recommended Actions
1. Fix T-8 so its assertion clause fully matches §4.5 variant-by-variant.
2. Replace the dangling `Z6-B1` citation with a concrete section/invariant reference.
3. Optionally capture `hooks.exported_env(run)` once in pseudocode to remove ambiguity.
