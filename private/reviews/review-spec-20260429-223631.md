# Hardcore Review: spec-multi-repo-workspace-model.md (iter-23)

- **Date**: 2026-04-29
- **Content Type**: Spec
- **Iteration**: 23
- **Reviewer Model(s)**: gpt-5.2 (orchestrated pass)
- **Verdict**: NEEDS WORK

## Scores

| # | Dimension | Score | Min-Reviewer | Notes |
|---|-----------|-------|--------------|-------|
| 1 | Correctness | 7/10 | gpt-5.2 | Step §3.5/5b decision table conflicts with B22 fail-closed routing for `OrphanPending` rows. |
| 2 | Completeness | 8/10 | gpt-5.2 | 5 target bug-fix threads are present with normative cross-links and tests. |
| 3 | Security | 8/10 | gpt-5.2 | Strong fd-anchored and fail-closed posture; deferred fuzz ratchet remains open. |
| 4 | Clarity | 7/10 | gpt-5.2 | Duplicate liveness/error routing statements diverge in one critical branch. |
| 5 | Architecture | 8/10 | gpt-5.2 | Lock hierarchy and reconcile ownership boundaries are explicit and consistent. |
| 6 | SRP | 8/10 | gpt-5.2 | Section responsibilities are mostly cleanly separated by lifecycle stage. |
| 7 | KISS/DRY | 7/10 | gpt-5.2 | Liveness routing duplicated across §3.5 and §3.6 with drift risk already realized. |

**Average**: 7.6/10

## Findings

### 🔴 Critical
1. [docs/specs/spec-multi-repo-workspace-model.md:489-494] — Decision table still says `row exists + alive => WorkspaceAlreadyExists (DUPLICATE)`, but B22 and §4.4 route `OrphanPending`+`Live|Inconclusive` to `WorkspaceBusy`. This can produce wrong retry/idempotency behavior in implementations that follow the table literally.

**Fix (exact replacement text):**

Replace lines 489-494 with:

```md
      | Dir at `<slug>/<run_id>/` exists | Registry row exists in pre-step-1.4 snapshot | Row status | `alive()` verdict (Z-10, §3.6 step 4) | Classification | Action |
      |---|---|---|---|---|---|
      | Yes | No | n/a | n/a | ORPHAN (crash mid-create or cleaned-row race) | Enqueue `(slug, run_id)` for asynchronous `OrphanReclaim` on the reconcile queue (§4.5); return `Error::WorkspaceBusyOrReclaiming`. |
      | Yes | Yes | `CleanupFailed` | n/a | ORPHAN (deterministic cleanup debt) | Enqueue `(slug, run_id)` for asynchronous `OrphanReclaim`; return `Error::WorkspaceBusyOrReclaiming`. |
      | Yes | Yes | `OrphanPending` | `Live` or `Inconclusive` | BUSY (fail-closed) | MUST NOT enqueue; return `Error::WorkspaceBusy`. |
      | Yes | Yes | `OrphanPending` | `Dead` | ORPHAN (positively disproved liveness) | Enqueue `(slug, run_id)` for asynchronous `OrphanReclaim`; return `Error::WorkspaceBusyOrReclaiming`. |
```

### 🟡 Important
1. [docs/specs/spec-multi-repo-workspace-model.md:2362-2371] — `ITER3-FOLLOWUP-4`/`-5` are tracked but still framed as broad future work without ownership/date gate. This is acceptable for draft maturity but should be promoted to explicit cross-spec action items before release branch cut.

### 🟢 Suggestions
1. [docs/specs/spec-multi-repo-workspace-model.md:600-648, 949-993] — Keep a single normative `alive()` definition (one anchor section) and turn other mentions into “MUST use §X alive()”; this removes DRY drift.

## Deferred Items Disposition

- **ITER3-FOLLOWUP-4 (`parent_dev_ino` migration):** **Non-blocking** for this spec revision. Runtime fallback is defined (TOFU/null-tolerant path in §3.6 step 5 and §5B.2 discriminator rules), so safety is preserved while migration mechanics are deferred.
- **ITER3-FOLLOWUP-5 (CVE-2022-21658 fuzz/property ratchet):** **Non-blocking** for spec merge, **recommended pre-GA security gate**. Core mitigation is already normative (`openat`/`unlinkat` fd-anchored removal), but fuzz ratchet materially improves regression resistance.

## Recommended Actions

1. Apply the §3.5 step 5b table replacement to remove routing contradiction.
2. Keep Appendix D followups as non-blockers for this draft; require owners and target milestone in spec #1/#2 planning.
